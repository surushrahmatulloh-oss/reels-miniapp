#!/bin/sh
# Auto push to GitHub + Render deploy after every commit
ROOT="$(git rev-parse --show-toplevel)"
cd "$ROOT" || exit 0
if command -v node >/dev/null 2>&1 && [ -f "$ROOT/scripts/push-github.mjs" ]; then
  node "$ROOT/scripts/push-github.mjs" || true
fi
